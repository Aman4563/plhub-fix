# PLhub Server Environment Variables
# Copy this file to .env and fill in your values

# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Connection
MONGODB_URL=mongodb://localhost:27017/plhub

# JWT Secrets (generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
TOKEN_SECRET=your_access_token_secret_here
REFRESH_TOKEN_SECRET=your_refresh_token_secret_here

# TMDB API
TMDB_API_KEY=your_tmdb_api_key_here

# Google OAuth
GOOGLE_CLIENT_ID=your_google_client_id_here

# reCAPTCHA
RECAPTCHA_SECRET_KEY=your_recaptcha_secret_key_here
# Set to "true" to bypass CAPTCHA verification in development (useful for testing)
BYPASS_CAPTCHA=false

# Email Configuration (for transactional emails: password reset, newsletter, contact confirmations)
# For Gmail: Enable 2FA and create an App Password at https://myaccount.google.com/apppasswords
# For other providers (SendGrid, Mailgun, etc.), update email.service.js accordingly
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_specific_password

# Admin Email (receives notifications about subscriptions, contact forms, job applications)
ADMIN_EMAIL=amanyadavmhow66@gmail.com

# Alternative: Use SMTP settings (uncomment and configure in email.service.js)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false

# Frontend URL (for email links and callbacks)
FRONTEND_URL=http://localhost:3000

# Redis (optional - will use memory cache if not set)
# REDIS_URL=redis://localhost:6379

